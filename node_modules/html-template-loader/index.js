var _ = require('lodash');
var render = require('./render');

module.exports = function(source,options){
    // console.log(source);
    var htmlContent = render(source)
    
    var template = _.template(htmlContent)
    console.log(template(),"-------")
    var str = template();
    return 'module.exports = ' + template;
};


